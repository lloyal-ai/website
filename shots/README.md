# Review screenshots — blog/working-memory-into-agents

Throwaway branch. Holds only the rendered-page screenshots referenced from the
body of the "How Lloyal turns the model's working memory into agents on the GPU"
PR, so they render inline on GitHub. Nothing here ships to the site.

Delete this branch once that PR is merged or closed.
